Suono splash «Entra» (un solo file):
  assets/sounds/bubble/bubble-dragon-pop.mp3

Per sostituirlo: rinomina il tuo MP3 con lo stesso nome e sovrascrivi questo file
(oppure cambia SPLASH_ENTER_SOUND_REL in index.html).

L'URL del file viene costruito in modo assoluto (anche su GitHub Pages) usando il percorso del manifest.
